# Never commit a real key. The deterministic replay path must work without one.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# Local development server; the app scaffold will decide the final convention.
PORT=3000
